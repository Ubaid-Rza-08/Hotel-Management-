apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: hotel-management
spec:
  selector:
    app: booking-service
  ports:
    - port: 8084
      targetPort: 8084
  type: ClusterIP