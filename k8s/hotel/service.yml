apiVersion: v1
kind: Service
metadata:
  name: hotel-listing-service
  namespace: hotel-management
spec:
  selector:
    app: hotel-listing-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP