apiVersion: v1
kind: Service
metadata:
  name: room-listing-service
  namespace: hotel-management
spec:
  selector:
    app: room-listing-service
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP